# [TODO]
def call_sample_function(chain, block_number, shard_id, sig_index):
    """Call the sample function and return signer address
    """
        
    return ''


# [TODO]
def call_validation_code_addr(chain, validation_code_addr):
    """Call validationCodeAddr on the main shard with 200000 gas, 0 value,
    the block_number concatenated with the sigIndex'th signature as input data gives output 1.
    """
    return True
